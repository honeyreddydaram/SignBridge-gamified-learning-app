# Top-level dependencies. See requirements.lock.txt for the exact,
# fully-resolved set actually verified working (pip freeze) on
# Python 3.13 / win_amd64, Sept 2026 — use that for reproducible installs:
#   pip install -r requirements.lock.txt

fastapi==0.141.1
uvicorn[standard]==0.53.0
sqlalchemy==2.0.53
alembic==1.20.0
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
# Only used for Postgres (docker-compose.yml) — local dev defaults to SQLite
psycopg2-binary==2.9.13
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
python-dotenv==1.2.3

# ML / CV
opencv-python-headless==5.0.0.93
mediapipe==1.0.1
numpy==2.5.3
scikit-learn==1.9.1
joblib==1.6.0
Pillow==12.3.0

# dataset download (HuggingFace parquet dataset, no auth needed)
pyarrow==25.0.1
pandas==3.0.5
requests==2.34.2

# dev / test
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.27.2
